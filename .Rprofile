# Custom Rprofile

# Some general options
options(papersize="letter")
options(editor="vim")
options(pager="less -X")

options(tab.width=4)
options(width=130)
options(graphics.record=TRUE)

if(interactive()){
	library(setwidth)
	library(vimcom)
}

.First <- function(){
	cat("Holy crap! You R in R\n")
}

.Last <- function(){
	cat("R died at  ", date(), "\n")
}
